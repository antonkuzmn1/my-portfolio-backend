version: '3.8'

services:
  app:
    build: .
    container_name: my-portfolio-backend
    ports:
      - "3002:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
